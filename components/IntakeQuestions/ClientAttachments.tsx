export default function ClientAttachments() {
  return (
    <div className="relative flex flex-col gap-2">
      This is where we would put the attachments the lead submits to the intake
      link.
    </div>
  );
}
